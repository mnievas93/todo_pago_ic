This repo contains ansible code to install JDK from OpenJDK

When cloning from github, simply run:

    rake

When using galaxy, simply run:

    ansible-galaxy install Azulinho.azulinho-java-openjdk-jdk
