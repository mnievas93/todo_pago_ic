This repo contains ansible code to install the Development Package group on RH

When cloning from github, simply run:

    rake

When using galaxy, simply run:

    ansible-galaxy install Azulinho.azulinho-devel-packages
